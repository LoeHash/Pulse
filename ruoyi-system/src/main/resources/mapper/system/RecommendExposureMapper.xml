<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.recommand.mapper.RecommendExposureMapper">

    <insert id="insertIgnoreBatch">
        INSERT IGNORE INTO tb_recommend_exposure
        (user_id, post_id, biz_date, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.postId}, #{item.bizDate}, #{item.createTime})
        </foreach>
    </insert>

</mapper>